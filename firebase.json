{
  "firestore": {
    "rules": "firestore.rules"
  },
  "functions": [
    {
      "codebase": "default",
      "source": "functions",
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run build",
        "npm --prefix \"$RESOURCE_DIR\" test"
      ],
      "ignore": [
        "node_modules",
        ".git"
      ]
    }
  ],
  "hosting": {
    "public": "build/web",
    "_comment_ignore": "Do NOT add \"**/.*\" here. This app ships .env as a declared Flutter asset (pubspec.yaml), so it builds to build/web/assets/.env, and that glob excludes it from the upload. main.dart calls dotenv.load() before runApp(), so a missing .env kills the app before it paints.",
    "ignore": ["firebase.json", "**/node_modules/**"],
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  },
  "emulators": {
    "auth": { "port": 9099 },
    "functions": { "port": 5001 },
    "firestore": { "port": 8080 },
    "ui": { "enabled": false }
  }
}
